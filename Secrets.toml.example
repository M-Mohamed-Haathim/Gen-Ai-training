# Rename this file to secrets.toml for LOCAL testing only.
# Never commit a real secrets.toml to GitHub.
# On Streamlit Cloud, paste this same content into:
# App settings -> Secrets (in the dashboard, not a file in your repo)

GROQ_API_KEY = "your-groq-api-key-here"
